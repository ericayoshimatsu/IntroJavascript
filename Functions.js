/*-----------------------
Functions
------------------------
*/

// var pos = 0;

function move (pos, byAmount)
{
    const newPos = pos + byAmount;
    return newPos;
}

finalPos = move(0, 10);

console.log(finalPos);